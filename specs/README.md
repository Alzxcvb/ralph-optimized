# Specs

One markdown file per topic of concern. These are the source of truth for how the project should behave.

Each Ralph instance reads these to understand requirements before implementing a task.
